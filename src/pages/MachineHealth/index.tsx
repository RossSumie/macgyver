import { NextPage } from 'next';
import MachineHealthTemplate from 'template/MachineHealth';

const MachineHealth: NextPage = () => {
    return <MachineHealthTemplate />;
};

export default MachineHealth;
